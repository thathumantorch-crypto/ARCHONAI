{
  "name": "archon-ai",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --config worker/wrangler.toml",
    "deploy": "wrangler deploy",
    "ingest": "ts-node scripts/ingest_markdown.ts",
    "test:api": "python test-local.py",
    "preview": "wrangler dev"
  },
  "dependencies": {
    "cross-fetch": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250129.0",
    "@types/node": "^25.6.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.0",
    "wrangler": "^4.83.0"
  }
}
